Commit the current code changes to git.

Commit message:
$ARGUMENTS

Steps:
1. Check git status and inspect the diff.
2. Stage all changes with `git add .`.
3. Commit using exactly the commit message provided above.
4. Do not push.
5. Show the resulting commit hash.

If no commit message was provided, ask the user for one.

Do not modify source files.